class bonusRound {
  constructor (winner) {
    this.player = winner;
  };

  newRound() {
    //display message that winner is winner
    //spin wheel once
    //
  }
}

if (typeof module !== 'undefined') {
  module.exports = bonusRound;
}